{
  "name": "@bonp/core",
  "version": "0.0.1",
  "description": "all shared stuff like redux, react-hooks not specific to dom/native, parsing for kindle etc",
  "private": true,
  "main": "build/index.js",
  "dependencies": {
    "@bonp/core": "0.0.1",
    "@reduxjs/toolkit": "^1.6.1",
    "typescript": "^4.3.5"
  },
  "scripts": {
    "build": "yarn tsc -p  ./tsconfig.json",
    "clean": "rm -rf build && rm tsconfig.tsbuildinfo"
  },
  "peerDependencies": {
    "react": "^17.0.2"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.15.4",
    "@babel/preset-typescript": "^7.15.0",
    "jest": "^27.1.0"
  }
}
